package com.prog2.labs;
/**
 * @author adinashby
 *
 */

public class LabFour {
	
	/**
	 * Write your test code below in the main (optional).
	 *
	 */
	public static void main(String[] args) {
		
	}
	
	/**
	 * 1) You are given an integer array height of length n. There are n vertical lines 
	 * drawn such that the two endpoints of the ith line are (i, 0) and (i, height[i]). 
	 * Find two lines that together with the x-axis form a container, such that 
	 * the container contains the most water. Return the maximum amount of water 
	 * a container can store. Notice that you may not slant the container.
	 * 
	 * @param height the input array
	 *
	 * @return result the maximum amount of water a container can store.
	 */
	
    public int containerWithMostWater(int[] height) {
        return -1;
    }
    
	/**
	 * 2) Given an integer array nums of length n and an integer target, find three integers
	 * in nums such that the sum is closest to target. Return the sum of the three integers.
	 * You may assume that each input would have exactly one solution.
	 * 
	 * @param nums the input array.
	 * @param target the target number.
	 *
	 * @return result the closest number to sum of 3.
	 */
    
    public int threeSumClosest(int[] nums, int target) {
        return -1;
    }
}
